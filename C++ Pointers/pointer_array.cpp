#include<iostream>
using namespace std;


int main(){
    int arr[10] = {3,5,6};

    // cout << "Address of first memory block is: " << arr << endl;
    // cout << "Address of first memory block using & operator is: " << &arr[0] << endl;
    //  cout << "Value of first memory block using * is: " << *arr << endl;
    // cout << "Address of second memory block is: " << arr+1 << endl;
    // cout << *arr + 1 << endl; // *arr = 3 then + 1 = 4
    //  cout << *(arr + 1) << endl; // arr+1 = first address and *(arr+1) = value at 1st address is 5
    //  cout << arr[1] << endl;
    //  cout << 1[arr] << " Aesa bh hta ha"<< endl;

    // arr = arr + 1; // can't modified !
    // cout << arr + 1 << endl; // but this is something we can to jump on next address
    
}